# Changelog

## v1.0.0
- Established public package layout (`src`, `docs`, `research`, `artifacts`).
- Promoted canonical spec to `docs/ttc_canonical_spec_v1.md`.
- Added OS-style build/run paths in root `Makefile`.
- Removed bundled binaries from source tree.
- Archived legacy workspace under `research/archive` without nested VCS metadata.
